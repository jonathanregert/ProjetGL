// Description :
//      test du court-circuit OR : opérande gauche true → droite non évaluée
//
// Resultats :
// true affiché sans erreur
{
    boolean a = true;
    boolean b = (1/0==0);
    print(a || b);
}
