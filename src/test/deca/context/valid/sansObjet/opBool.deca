// Description :
//    Evaluation de differents expressions boolean
//
// Resultats :
// ok

{
    int i = 10;
    float f = 3.14;
    boolean b = true;
    boolean result;

    f = - f * (i + 2);

    if (!(i < 0) && (b || f == 0.0)) {
        println("La logique est correcte.");
    } else {
        println("Condition fausse.");
    }

    while (i > 0 && b) {
        i = i - 1;
        if (i == 5) {
            b = false;
        }
    }
}