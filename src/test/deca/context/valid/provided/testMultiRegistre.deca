{
    int a = 3;
    int b = 2;
    int x = 11;
    int y = 7;
    int z = 9;

    int c = 0;

    int w = (a + b) + (x + y);
    // a -> R1
    // push R1
    // b -> R1
    // pop R2

    // add R2 , R1 -> R1
    // push R1

    // x -> R1
    // push R1
    // y -> R1
    // pop R2

    // add R2, R1 -> R1
    // push R1

    // pop R2
    // add R2, R1 -> R1

    // ------------ variante ------------

    -r 5
    3 registres dispo + 2 bloqués
    int w = (a + b) + (x + y) + (z + c);

    // a -> R1
    // b -> R2

    // add R2 , R1 -> R1

    // x -> R2
    // y -> R3

    // add R3, R2 -> R2

    // add R3, R2 -> R2

    // z -> R3
    // push R3
    // c -> R3
    // pop
    // add R3, R2 -> R2

    x1 + x2 + x3 + x4 + x6 + x7 + x8 + x9 // -r 4 , 2 vraies 
    (x1 + x2) + ((x3 + x4) + (x6 + (x7 + x8)) + x9) // -r 7

    (x + y) -> 2 registres à calculer , 1 registre pour stocker
    (expr1 op expr2) -> cout = 
    
                +
            +       +
          +   +
    // après fils g -> -- param droit
    // 
    

    // descente -> décremente registre par thread
                    si <= 0 dans l'enfant on utilise la pile
    // remonte -> dans opérateur 
                  (si <= 0 on pop sinon utilise le registre), 
                  on incrémente

    (x1 + x2) + ((x3 + x4) + (x6 + (x7 + x8)) + x9)


    // ------------ exemple avec r -4 ------------
    (x3 + x4) + (x6 + (x7 + x8))


    // available 2 
    x3 -> R1
    // available 1
    x4 -> R2
    PUSH R2
    ADD R2, R1 -> R1
    PUSH R1



    // ------------ exemple critique -r 4 ------------
    x + y;
    available 2 
    // x -> R1
    available 1
    // y -> R2
    // ADD R2, R1;
    available 1

        +
    x       y

    x + (y + z);
    available 2 
    // x -> R1
    // PUSH R1
    available 2 
    // y -> R1
    available 1
    // z -> R2
    // ADD

            +
        *       +
            y       z







    x + (y + (z + (w + j)));
    
    avec -r 5 donc les dispos sont : R2, R3, R4

    // available 3
    LOAD x R2

    // available 2
    LOAD y R3
    PUSH R3

    // available 2
    LOAD z R3
    PUSH R3

    // available 2
    LOAD w R3
    // available 1
    LOAD j R4
    ADD R3 R4
    // available 2
    
    POP z R3
    ADD R4 R3
    // available 2

    POP y R4
    ADD R4 R3
    // available 3

    ADD R3 R2


    avec -r 4 donc les dispos sont : R2, R3
    LOAD x R2
    PUSH R2

    LOAD y R2
    PUSH R2

    LOAD z R2
    PUSH R2

    LOAD w R2
    LOAD j R3
    ADD R2 R3

    POP z R2
    ADD R2 R3

    POP y R2
    ADD R2 R3

    POP x R2
    ADD R3 R2

// ----------- LOGIQUE -------------
    // ordre croissant
    // pic on inverse l'ordre
    // ordre décroissant
    // 
    // cas terminal tjs stocker dans R2 : OP R3 R2
// ----------- LOGIQUE -------------


    x + (y + (z + (w + j)));
    avec -r 6 donc les dispos sont : R2, R3, R4, R5

    // available 4
    LOAD x R2

    // available 3
    LOAD y R3

    // available 2
    LOAD z R4
    PUSH R4

    // available 2
    LOAD w R4
    // available 1
    LOAD j R5
    ADD R4 R5
    increase available
    
    // available 2
    // left not in register
    POP z R4
    ADD R5 R4

    // available 3
    // left is in register
    ADD R4 R3
    increase available

    // available 4
    // left is in register
    ADD R3 R2



    // /!\ increase available seulement si left is in register
}