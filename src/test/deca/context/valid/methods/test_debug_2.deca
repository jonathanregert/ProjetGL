class A { void m() {} }
{
  A a = new A();
  int x;
  a.m();     // LOAD R0, R1
  x = 7;     // si ton Assign/Init réutilise R1 sans re-LOAD (ça arrive dans certains codegens optimistes)
  println(x);
}
