// Description :
//      test du court-circuit AND : opérande gauche false → droite non évaluée
//
// Resultats :
// false affiché sans erreur
{
    boolean a = false;
    boolean b = (1/0==0); // normalement non évalué
    print(a && b);
}
