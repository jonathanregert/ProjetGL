// Description :
//  Test d'une expression booléenne simple avec comparaison et opérations arithmétiques.
// Resultats :
// true affiché
{
    int a = 4;
    int b = 2;
    boolean r;

    r = (a * b - 3) > (b + 1);
    println(r);
}
